/*
const { SlashCommandBuilder } = require("@discordjs/builders");
const { getCrabeEmbed } = require("../crabe_utils");
const Crabe = require("../../../database/models/model_Crabe");
const Village = require("../../../database/models/model_Village");
module.exports = {
  data: new SlashCommandBuilder()
    .setName("crabes")
    .setDescription("Affiche la liste de crabe ayant le nom donné")
    .addStringOption((option) =>
      option
        .setName("Nom_crabe")
        .setDescription("Nom du crabe recherché")
        .setRequired(true)
    ),
  async execute(interaction) {},
};
*/
